{
  "manifest_version": 3,
  "name": "Free Audio Chrome Transcribator",
  "version": "0.1.0",
  "description": "One click records the active tab with a video call (participant audio + your mic) and sends the recording to a local Whisper server for transcription.",
  "permissions": ["tabCapture", "offscreen", "storage", "downloads", "activeTab"],
  "host_permissions": ["http://127.0.0.1/*", "http://localhost/*"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_title": "Free Audio Chrome Transcribator"
  }
}
